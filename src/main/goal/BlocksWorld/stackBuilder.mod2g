use blocksworld as knowledge.
use tower as goals.
use builderBeliefs as beliefs.
use build.
use clear.

exit=nogoals.

module stackBuilder {
	if goal(tower(T)), bel(not(tower(T))), bel(available(T)) then build(T).
	if bel(not(clear)) then clear + insert(clear).	
}
