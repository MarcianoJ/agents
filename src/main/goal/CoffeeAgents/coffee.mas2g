define maker as agent {
	use machine as main module.
	use coffeeMakerInit as init module.
	use coffeeEvents as event module.
} 

define grinder as agent {
	use machine as main module..
	use grinderInit as init module.
	use coffeeEvents as event module.
} 

launchpolicy {
	launch maker.
	launch grinder.
}
