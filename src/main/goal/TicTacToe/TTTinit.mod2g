use TTTknowledge as knowledge.
use actions as actionspec.

module init {
	% set a goal to win.
	if bel( me(Player) ) then adopt( winner(Player) ).
	% if I am X player, then make a first random move.
	if true then occupy(Pos).
	if true then insert(turn(unknown)).
}
