use TTTknowledge as knowledge.
use actions as actionspec.

module init {
	% set a goal to win.
	if true then insert(me(o)) + adopt(winner(o)).
	% if I am X player, then make a first random move.
	if true then occupy(Pos) + insert(turn(unknown)).
}
