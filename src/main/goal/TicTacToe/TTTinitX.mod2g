use TTTknowledge as knowledge.
use actions as actionspec.

module init {
	% set a goal to win.
	if true then insert(me(x)) + adopt(winner(x)).
	% check who's turn it is first
	if percept(turn(X)) then insert(turn(X)).
}
