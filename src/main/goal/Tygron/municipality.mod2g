use dummy as knowledge.
use tygron as actionspec.
use somegoal as goals.

exit=nogoals.

module helloWorld {
	% sell the first piece of land.
	if bel(lands([land(ID,Owner, Poly)|MORE]), 
		stakeholders(L), 
		member(['Inhabitants',BuyerID,_],L))
		then map_sell_land(BuyerID, Poly,400.0).

}
